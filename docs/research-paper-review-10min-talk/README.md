# Research Paper Review Talk

LaTeX Beamer source for a 10-minute project talk about the
`research-paper-review` Codex skill.

Generated PDFs are intentionally not committed. To rebuild locally:

```bash
cd docs/research-paper-review-10min-talk
make
```

Use `make clean` to remove generated PDFs and LaTeX build artifacts.
